<% content_for :title, "Editing detail" %>

<h1>Editing detail</h1>

<%= render "form", detail: @detail %>

<br>

<div>
  <%= link_to "Show", @detail, class: "btn btn-secondary" %> 
   <%= link_to "Delete", @detail,
    data: { turbo_method: :delete, turbo_confirm: "Are you sure?" } ,  class:"btn btn-danger btn-sm" %>

  <%= link_to "Back", details_path,class: "btn btn-dark" %>
</div>
